public class Test1 {
    void renderBanner() {
        if ((pla<caret>tform.toUpperCase().indexOf("MAC") > -1) &&
                (browser.toUpperCase().indexOf("IE") > -1) &&
                wasInitialized() && resize > 0) {
            // do something
        }
    }
}